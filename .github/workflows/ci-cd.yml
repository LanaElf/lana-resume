name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 24
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Vitest tests
        run: npx vitest run

      # Билд для Beget (корень)
      - name: Build static version for Beget
        run: npm run generate
        env:
          NUXT_APP_BASE_URL: '/'

      - name: Copy assets to .output/public/_nuxt (for Beget)
        run: cp -r assets/* .output/public/_nuxt/ || true  # || true чтобы не падать, если assets пустая

      - name: Upload build artifacts for Beget
        uses: actions/upload-artifact@v4
        with:
          name: build-output-beget
          path: .output/public

      # Билд для GitHub Pages (поддиректория)
      - name: Build static version for GitHub Pages
        run: npm run generate
        env:
          NUXT_APP_BASE_URL: '/lana-resume/'

      - name: Upload build artifacts for GitHub Pages
        uses: actions/upload-artifact@v4
        with:
          name: build-output-gh-pages
          path: .output/public

  deploy:
    needs: build-test
    runs-on: ubuntu-latest
    if: ${{ success() }}

    steps:
      - name: Download build artifacts for Beget
        uses: actions/download-artifact@v4
        with:
          name: build-output-beget
          path: .output/public

      - name: Deploy to Beget via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.BEGET_HOST }}
          username: ${{ secrets.BEGET_FTP_USER }}
          password: ${{ secrets.BEGET_FTP_PASSWORD }}
          local-dir: .output/public/
          server-dir: /

      - name: Notify Telegram on failure
        if: failure()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🚨 *Beget Deploy Failed* 🚨
            Репозиторий: ${{ github.repository }}
            Ветка: ${{ github.ref }}
            Коммит: ${{ github.sha }}
            Автор: ${{ github.actor }}
            Workflow: ${{ github.workflow }}
            Подробнее: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          format: Markdown

  deploy-gh-pages:
    needs: build-test
    runs-on: ubuntu-latest
    if: ${{ success() }}

    permissions:
      contents: write  # Добавлено для push в gh-pages
      pages: write
      id-token: write

    steps:
      - name: Download build artifacts for GitHub Pages
        uses: actions/download-artifact@v4
        with:
          name: build-output-gh-pages
          path: .output/public

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .output/public
          destination_dir: lana-resume # Поддиректория на GitHub Pages
          publish_branch: gh-pages
          allow_empty_commit: true  # Для создания ветки, если не существует
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      - name: Notify Telegram on GH Pages failure
        if: failure()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🚨 *GH Pages Deploy Failed* 🚨
            Репозиторий: ${{ github.repository }}
            Ветка: ${{ github.ref }}
            Коммит: ${{ github.sha }}
            Автор: ${{ github.actor }}
            Workflow: ${{ github.workflow }}
            Подробнее: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          format: Markdown